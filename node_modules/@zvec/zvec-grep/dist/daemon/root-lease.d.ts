export type RootLease = {
    readonly root: string;
    release(): Promise<void>;
};
export declare class RootLeaseManager {
    readonly instanceToken: `${string}-${string}-${string}-${string}-${string}`;
    private readonly leases;
    acquire(root: string): Promise<RootLease>;
    close(): Promise<void>;
    private createLease;
    private release;
    private startHeartbeat;
    private removeOwnedLease;
}
