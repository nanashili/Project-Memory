import { type ZvecGrepInfoResult } from "../engine/service/index.js";
import type { CreateZvecGrepOptions } from "../engine/service/types.js";
import type { EmbeddingModelLoadRequest, EmbeddingModelPool } from "./model-pool.js";
import { RootRuntime } from "./root-runtime.js";
import { RootLeaseManager } from "./root-lease.js";
export type RuntimeManagerOptions = {
    modelPool: EmbeddingModelPool;
    serviceOptions?: CreateZvecGrepOptions;
    readSessionIdleTtlMs?: number;
    runtimeIdleTtlMs?: number;
    onRuntimeEvicted?: (canonicalRoot: string) => void | Promise<void>;
    rootLeaseManager?: RootLeaseManager;
    createRuntime?: (input: {
        canonicalRoot: string;
        modelLoadRequest?: EmbeddingModelLoadRequest;
        modelPool: EmbeddingModelPool;
    }) => RootRuntime | Promise<RootRuntime>;
};
export type RuntimeManagerSnapshot = {
    activeRuntimes: number;
};
export declare class RuntimeManager {
    private readonly options;
    private readonly runtimes;
    private readonly creating;
    private readonly aliases;
    private readonly rootLeaseManager;
    private readonly idleTimers;
    private closed;
    constructor(options: RuntimeManagerOptions);
    get instanceToken(): string;
    activate(requestedRoot: string): Promise<RootRuntime>;
    activateForIndex(requestedRoot: string): Promise<RootRuntime>;
    peek(requestedRoot: string): Promise<RootRuntime | undefined>;
    getByCanonicalRoot(canonicalRoot: string): RootRuntime | undefined;
    getByRequestedRoot(requestedRoot: string): RootRuntime | undefined;
    snapshot(): RuntimeManagerSnapshot;
    evict(canonicalRoot: string): Promise<boolean>;
    close(): Promise<void>;
    private getOrCreate;
    private createRuntime;
    private touchRuntime;
    private evictIfIdle;
}
export declare function inspectRoot(requestedRoot: string, serviceOptions?: CreateZvecGrepOptions, includeStatus?: boolean): Promise<ZvecGrepInfoResult>;
export declare function resolveRequestedRoot(requestedRoot: string, writable: boolean): Promise<string>;
