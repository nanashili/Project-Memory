import { randomUUID } from "node:crypto";
import { createEmbeddingModelForIdentity, embeddingModelPoolKeyForIdentity, } from "../engine/service/zvec-grep.js";
export class EmbeddingModelPool {
    entries = new Map();
    idleTtlMs;
    maxLoadedModels;
    createModel;
    keyForRequest;
    closed = false;
    closePromise;
    logger;
    constructor(options = {}) {
        this.idleTtlMs = options.idleTtlMs ?? 15 * 60_000;
        this.maxLoadedModels = options.maxLoadedModels ?? 1;
        this.createModel =
            options.createModel ??
                ((request) => createEmbeddingModelForIdentity(request.model, {
                    ...options.serviceOptions,
                    ...request.runtime,
                }));
        this.keyForRequest =
            options.keyForRequest ??
                ((request) => embeddingModelPoolKeyForIdentity(request.model, {
                    ...options.serviceOptions,
                    ...request.runtime,
                }));
        this.logger = options.logger;
    }
    async acquire(request) {
        if (this.closed) {
            throw new Error("Embedding model pool is closed.");
        }
        const key = this.keyFor(request);
        let entry = this.entries.get(key);
        if (!entry) {
            entry = {
                key,
                logIdentity: randomUUID(),
                leases: 0,
                lastUsedAt: Date.now(),
                retired: false,
            };
            this.entries.set(key, entry);
        }
        if (entry.idleTimer) {
            clearTimeout(entry.idleTimer);
            entry.idleTimer = undefined;
        }
        if (!entry.model) {
            if (!entry.loading) {
                this.logger?.event("model.load", { model_id: entry.logIdentity });
                entry.loading = Promise.resolve(this.createModel(request));
            }
            try {
                entry.model = await entry.loading;
            }
            catch (error) {
                this.entries.delete(key);
                throw error;
            }
            finally {
                entry.loading = undefined;
            }
        }
        else {
            this.logger?.event("model.cache_hit", { model_id: entry.logIdentity });
        }
        if (this.closed) {
            entry.retired = true;
            throw new Error("Embedding model pool is closed.");
        }
        entry.leases += 1;
        entry.lastUsedAt = Date.now();
        try {
            await this.trimIdleEntries(key);
        }
        catch (error) {
            this.release(entry);
            throw error;
        }
        if (this.closed) {
            this.release(entry);
            throw new Error("Embedding model pool is closed.");
        }
        let released = false;
        return {
            model: entry.model,
            key,
            release: () => {
                if (released) {
                    return;
                }
                released = true;
                this.release(entry);
            },
        };
    }
    keyFor(request) {
        return this.keyForRequest(request);
    }
    snapshot() {
        let activeLeases = 0;
        let loaded = 0;
        for (const entry of this.entries.values()) {
            activeLeases += entry.leases;
            if (entry.model) {
                loaded += 1;
            }
        }
        return { loaded, activeLeases };
    }
    close() {
        if (!this.closePromise) {
            this.closed = true;
            this.closePromise = this.closeEntries();
        }
        return this.closePromise;
    }
    async closeEntries() {
        const loadings = [...this.entries.values()].flatMap((entry) => entry.loading ? [entry.loading] : []);
        await Promise.allSettled(loadings);
        const disposals = [];
        for (const entry of this.entries.values()) {
            if (entry.idleTimer) {
                clearTimeout(entry.idleTimer);
            }
            entry.retired = true;
            if (entry.leases === 0 && entry.model) {
                disposals.push(entry.model.dispose());
            }
        }
        await Promise.all(disposals);
        for (const [key, entry] of this.entries) {
            if (entry.leases === 0) {
                this.entries.delete(key);
            }
        }
    }
    release(entry) {
        entry.leases = Math.max(0, entry.leases - 1);
        entry.lastUsedAt = Date.now();
        if (entry.leases > 0) {
            return;
        }
        if (entry.retired || this.closed || this.idleTtlMs === 0) {
            void this.disposeEntry(entry);
            return;
        }
        const usedAt = entry.lastUsedAt;
        entry.idleTimer = setTimeout(() => {
            if (entry.leases === 0 && entry.lastUsedAt === usedAt) {
                void this.disposeEntry(entry);
            }
        }, this.idleTtlMs);
        entry.idleTimer.unref?.();
    }
    async trimIdleEntries(exceptKey) {
        const loaded = [...this.entries.values()].filter((entry) => entry.model);
        if (loaded.length <= this.maxLoadedModels) {
            return;
        }
        const candidates = loaded
            .filter((entry) => entry.key !== exceptKey && entry.leases === 0)
            .sort((left, right) => left.lastUsedAt - right.lastUsedAt);
        while (this.snapshot().loaded > this.maxLoadedModels &&
            candidates.length > 0) {
            await this.disposeEntry(candidates.shift());
        }
    }
    async disposeEntry(entry) {
        if (entry.leases > 0 || !entry.model) {
            return;
        }
        if (entry.idleTimer) {
            clearTimeout(entry.idleTimer);
            entry.idleTimer = undefined;
        }
        const model = entry.model;
        entry.model = undefined;
        this.entries.delete(entry.key);
        await model.dispose();
        this.logger?.event("model.evicted", {
            model_id: entry.logIdentity,
        });
    }
}
//# sourceMappingURL=model-pool.js.map