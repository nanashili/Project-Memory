export type ClosableReadHandle = {
    close(): void | Promise<void>;
};
export type WorkspaceReadSessionCacheOptions<T extends ClosableReadHandle> = {
    open: () => T | Promise<T>;
    idleTtlMs?: number;
    serializeOperations?: boolean;
};
export declare class WorkspaceReadSessionCache<T extends ClosableReadHandle> {
    private readonly options;
    private handle?;
    private openPromise?;
    private activeReaders;
    private lastReadAt;
    private idleTimer?;
    private operationTail;
    private closePromise?;
    private closeResolve?;
    private closed;
    constructor(options: WorkspaceReadSessionCacheOptions<T>);
    withRead<R>(operation: (handle: T) => Promise<R>): Promise<R>;
    close(): Promise<void>;
    snapshot(): {
        open: boolean;
        activeReaders: number;
    };
    private getOrOpen;
    private runSerial;
    private scheduleIdleClose;
    private closeHandle;
}
