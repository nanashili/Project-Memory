export type ChangeKind = "created" | "changed" | "deleted";
export type ChangeSetSnapshot = {
    touchedFiles: string[];
    rescanDirectories: string[];
    deletedPrefixes: string[];
    forceFullReconcile: boolean;
};
export type ChangeSetOptions = {
    root?: string;
    maxChangedPaths?: number;
};
export declare class ChangeSet {
    private readonly touchedFiles;
    private readonly rescanDirectories;
    private readonly deletedPrefixes;
    private forceFullReconcile;
    private readonly root?;
    private readonly maxChangedPaths;
    constructor(options?: ChangeSetOptions);
    add(path: string, kind: ChangeKind, isDirectory?: boolean): void;
    requireFullReconcile(): void;
    merge(other: ChangeSetSnapshot): void;
    snapshot(): ChangeSetSnapshot;
    get size(): number;
    get empty(): boolean;
    private collapsePaths;
    private enforcePathBudget;
    private pathCoveredBy;
    private parentScope;
}
