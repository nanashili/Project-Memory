export const EMBEDDING_ENVIRONMENT_META_KEY = "io.zvec-grep/embedding-environment";
export function embeddingEnvironmentFromRequestMeta(meta) {
    if (typeof meta !== "object" || meta === null)
        return undefined;
    const value = meta[EMBEDDING_ENVIRONMENT_META_KEY];
    if (typeof value !== "string")
        return undefined;
    const normalized = value.trim();
    return normalized.length > 0 ? normalized : undefined;
}
//# sourceMappingURL=request-metadata.js.map