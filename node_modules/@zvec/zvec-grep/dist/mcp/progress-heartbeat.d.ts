import type { ProgressNotification } from "@modelcontextprotocol/server";
export declare const REMOTE_AUTHORIZATION_HEARTBEAT_MS = 15000;
export declare const LONG_RUNNING_MCP_TIMEOUT_MS = 2147483647;
type ProgressHeartbeatContext = {
    mcpReq: {
        _meta?: {
            progressToken?: string | number;
        };
        signal: AbortSignal;
        notify: (notification: ProgressNotification) => Promise<void>;
    };
};
export declare function withProgressHeartbeat<T>(context: ProgressHeartbeatContext, operation: () => Promise<T>, options: {
    intervalMs?: number;
    message: string;
}): Promise<T>;
export {};
