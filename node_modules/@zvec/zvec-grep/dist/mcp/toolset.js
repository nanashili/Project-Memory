export const MCP_TOOLSET_ENV = "ZVEC_GREP_MCP_TOOLSET";
export const DEFAULT_MCP_TOOLSET = "agent";
export function parseMcpToolset(value) {
    if (value === "agent" || value === "full")
        return value;
    throw new Error(`Unsupported MCP toolset "${value}". Expected "agent" or "full".`);
}
export function resolveMcpToolset(explicitValue, environmentValue) {
    const value = explicitValue ?? environmentValue;
    return value === undefined ? DEFAULT_MCP_TOOLSET : parseMcpToolset(value);
}
//# sourceMappingURL=toolset.js.map