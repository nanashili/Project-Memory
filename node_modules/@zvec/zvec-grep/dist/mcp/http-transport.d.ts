import type { IncomingMessage, ServerResponse } from "node:http";
import type { ZvecGrepDaemonBackend, ZvecGrepMcpServerOptions } from "./tools.js";
export type McpHttpEndpointOptions = {
    legacySessionIdleTtlMs?: number;
    maxLegacySessions?: number;
    modernOnly?: boolean;
};
export declare class McpHttpEndpoint {
    private readonly backend;
    private readonly version;
    private readonly sessions;
    private readonly initializing;
    private readonly serverOptions;
    private readonly modern;
    private readonly modernNodeHandler;
    private readonly legacySessionIdleTtlMs;
    private readonly maxLegacySessions;
    private readonly modernOnly;
    constructor(backend: ZvecGrepDaemonBackend, version: string, serverOptions?: Readonly<ZvecGrepMcpServerOptions>, endpointOptions?: Readonly<McpHttpEndpointOptions>);
    handlePost(request: IncomingMessage, response: ServerResponse, body: unknown): Promise<void>;
    handleSessionRequest(request: IncomingMessage, response: ServerResponse): Promise<void>;
    close(): Promise<void>;
    private handleLegacyPost;
    private expireIdleSessions;
    private withActiveSession;
    private forget;
}
