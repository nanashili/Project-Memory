import { Client } from "@modelcontextprotocol/client";
import { Server } from "@modelcontextprotocol/server";
import { type DaemonControlStatus } from "../daemon/server-controller.js";
import type { McpToolset } from "./toolset.js";
export declare function runStdioBootstrapBridge(options: {
    cliPath: string;
    version: string;
    home?: string;
    tokenFile?: string;
    listen?: string;
    mcpToolset?: McpToolset;
}): Promise<void>;
export declare function shouldStopStdioBridge(connected: DaemonControlStatus, current: DaemonControlStatus): boolean;
export declare function registerStdioBridgeElicitationForwarding(upstream: Client, downstreamServer: () => Server | undefined): void;
