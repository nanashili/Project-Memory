import type { EmbeddingModel } from "../../models/index.js";
import type { WorkspaceIndexStorage } from "../../storage/index.js";
import type { WorkspaceIndexInfo, EntitySearchDiagnosis, SearchPlan, SearchPlanResult } from "../../types.js";
type SearchContext = {
    workspaceIndex: WorkspaceIndexInfo;
    storage: WorkspaceIndexStorage;
    embeddingModel?: EmbeddingModel;
};
export declare function searchWorkspaceIndex(plan: SearchPlan, ctx: SearchContext): Promise<SearchPlanResult>;
export declare function diagnoseEntitySearch(query: string, entityId: string, ctx: SearchContext): Promise<EntitySearchDiagnosis>;
export declare function diagnoseFileSearch(query: string, absolutePath: string, ctx: SearchContext): Promise<EntitySearchDiagnosis | null>;
export {};
