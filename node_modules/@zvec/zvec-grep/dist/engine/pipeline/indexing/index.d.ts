import type { EmbeddingModel } from "../../models/index.js";
import type { WorkspaceIndexStorage } from "../../storage/index.js";
import type { WorkspaceIndexStatus, WorkspaceIndexInfo, FileInfo, IndexProgress, IndexResult } from "../../types.js";
export type IndexContext = {
    workspaceIndex: WorkspaceIndexInfo;
    storage: WorkspaceIndexStorage;
    embeddingModel: EmbeddingModel;
    embeddingConcurrency?: number;
    onProgress?: (progress: IndexProgress) => void;
    signal?: AbortSignal;
};
export declare function indexWorkspace(ctx: IndexContext): Promise<IndexResult>;
export declare function indexWorkspacePaths(ctx: IndexContext, changedPaths: readonly string[]): Promise<IndexResult>;
export declare function getWorkspaceIndexStatus(workspaceIndex: WorkspaceIndexInfo, storedFiles: readonly FileInfo[]): Promise<WorkspaceIndexStatus>;
