export type ResolveEmbeddingReferenceOptions = {
    explicit?: string;
    existing?: string;
    globalDefault?: string;
    environment?: NodeJS.ProcessEnv;
    fallback?: string;
};
export declare function resolveEmbeddingReference(options: ResolveEmbeddingReferenceOptions): string | undefined;
