import { parentPort, workerData } from "node:worker_threads";
import { loadModel2VecTokenizer } from "./model2vec-tokenizer.js";
import { embedModel2VecTexts, staticEmbeddingTableFromWorkerData, } from "./model2vec-runtime.js";
const port = parentPort;
if (!port) {
    throw new Error("Model2Vec worker requires a parent port");
}
const data = workerData;
const tokenizer = await loadModel2VecTokenizer(data.tokenizerSource);
const table = staticEmbeddingTableFromWorkerData(data);
port.postMessage({ type: "ready" });
port.on("message", async (request) => {
    try {
        const result = await embedModel2VecTexts(request.texts, tokenizer, table, data.maxInputTokens, data.normalize);
        const flatVectors = new Float32Array(result.vectors.length * data.dimension);
        for (const [index, vector] of result.vectors.entries()) {
            flatVectors.set(vector, index * data.dimension);
        }
        const response = {
            type: "result",
            id: request.id,
            vectors: flatVectors.buffer,
            vectorCount: result.vectors.length,
            truncated: result.truncated,
        };
        port.postMessage(response, [flatVectors.buffer]);
    }
    catch (error) {
        const cause = error instanceof Error ? error : new Error(String(error));
        port.postMessage({
            type: "error",
            id: request.id,
            error: {
                name: cause.name,
                message: cause.message,
                stack: cause.stack,
            },
        });
    }
});
//# sourceMappingURL=model2vec-worker.js.map