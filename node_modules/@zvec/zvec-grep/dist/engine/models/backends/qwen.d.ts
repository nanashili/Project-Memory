import type { Content } from "../../types.js";
import { BaseEmbeddingModel, type CreateEmbeddingModelOptions, type EmbeddingModelInfo, type EmbeddingResult, type NormalizedEmbeddingOptions } from "../embeddings.js";
import type { QwenMultimodalEmbeddingCatalogEntry, QwenTextEmbeddingCatalogEntry } from "../catalog.js";
type QwenDependencies = {
    fetch: typeof globalThis.fetch;
};
type QwenTextEmbeddingSpec = {
    displayName: string;
    errorCodePrefix: string;
};
type QwenTextEmbeddingV4CatalogEntry = Extract<QwenTextEmbeddingCatalogEntry, {
    model: "text-embedding-v4";
}>;
type Qwen37TextEmbeddingCatalogEntry = Extract<QwenTextEmbeddingCatalogEntry, {
    model: "qwen3.7-text-embedding";
}>;
declare abstract class QwenTextEmbeddingModel extends BaseEmbeddingModel {
    readonly info: EmbeddingModelInfo;
    private readonly entry;
    private readonly apiKey;
    private readonly endpoint;
    private readonly displayName;
    private readonly errorCodePrefix;
    private readonly dependencies;
    constructor(entry: QwenTextEmbeddingCatalogEntry, spec: QwenTextEmbeddingSpec, options: CreateEmbeddingModelOptions, dependencies: Partial<QwenDependencies>);
    protected doEmbed(contents: readonly Content[], options: NormalizedEmbeddingOptions): Promise<EmbeddingResult>;
    private errorCode;
}
export declare class QwenTextEmbeddingV4Model extends QwenTextEmbeddingModel {
    constructor(entry: QwenTextEmbeddingV4CatalogEntry, options: CreateEmbeddingModelOptions, dependencies?: Partial<QwenDependencies>);
}
export declare class Qwen37TextEmbeddingModel extends QwenTextEmbeddingModel {
    constructor(entry: Qwen37TextEmbeddingCatalogEntry, options: CreateEmbeddingModelOptions, dependencies?: Partial<QwenDependencies>);
}
export declare class Qwen3VlEmbeddingModel extends BaseEmbeddingModel {
    readonly info: EmbeddingModelInfo;
    private readonly entry;
    private readonly apiKey;
    private readonly endpoint;
    private readonly dependencies;
    constructor(entry: QwenMultimodalEmbeddingCatalogEntry, options: CreateEmbeddingModelOptions, dependencies?: Partial<QwenDependencies>);
    protected doEmbed(contents: readonly Content[], options: NormalizedEmbeddingOptions): Promise<EmbeddingResult>;
}
export {};
