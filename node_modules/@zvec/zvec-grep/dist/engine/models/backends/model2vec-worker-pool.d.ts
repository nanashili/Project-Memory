import type { EmbeddingResult } from "../embeddings.js";
import type { Model2VecWorkerData } from "./model2vec-runtime.js";
export declare class Model2VecWorkerPool {
    private readonly data;
    private readonly workerUrl;
    private disposed;
    private nextJobId;
    private readonly maxWorkers;
    private readonly queue;
    private readonly slots;
    constructor(data: Model2VecWorkerData, maxWorkers?: number, workerUrl?: URL);
    start(): Promise<void>;
    run(texts: readonly string[], signal?: AbortSignal): Promise<EmbeddingResult>;
    dispose(): Promise<void>;
    private spawnWorker;
    private handleMessage;
    private dispatch;
    private postJob;
    private drain;
    private nextQueuedJob;
    private failSlot;
    private resolveJob;
    private rejectJob;
    private ensureNotDisposed;
}
