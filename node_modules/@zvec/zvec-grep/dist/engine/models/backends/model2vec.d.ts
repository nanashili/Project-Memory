import type { Content } from "../../types.js";
import { BaseEmbeddingModel, type CreateEmbeddingModelOptions, type EmbeddingModelInfo, type EmbeddingResult, type NormalizedEmbeddingOptions } from "../embeddings.js";
import type { Model2VecEmbeddingCatalogEntry } from "../catalog.js";
import { type StaticEmbeddingTable, type TokenizerLike } from "./model2vec-runtime.js";
type Model2VecDependencies = {
    loadTokenizer(repo: string, options: {
        cache_dir: string;
        revision: string;
        local_files_only?: boolean;
    }): Promise<TokenizerLike>;
    loadSafetensors(path: string, tensorName: string, dimension: number): Promise<StaticEmbeddingTable>;
    download(url: string, destination: string, onProgress?: (progress: {
        downloadedBytes: number;
        totalBytes?: number;
    }) => void): Promise<void>;
};
export declare class Model2VecEmbeddingModel extends BaseEmbeddingModel {
    private readonly entry;
    readonly info: EmbeddingModelInfo;
    private readonly modelCacheDir;
    private readonly dependencies;
    private tokenizer;
    private staticTable;
    private workerPool;
    private loadPromise;
    private readonly useWorkerPool;
    private disposed;
    constructor(entry: Model2VecEmbeddingCatalogEntry, options: CreateEmbeddingModelOptions, dependencies?: Partial<Model2VecDependencies>);
    protected doEmbed(contents: readonly Content[], options: NormalizedEmbeddingOptions): Promise<EmbeddingResult>;
    private embedBatch;
    dispose(): Promise<void>;
    private ensureLoaded;
    private loadModel;
    private resolveModelPath;
    private resolveTokenizerSource;
    private resolveCachedFile;
    private embedTexts;
    private ensureNotDisposed;
}
export {};
