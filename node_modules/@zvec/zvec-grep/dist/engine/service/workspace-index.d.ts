import type { EmbeddingModel } from "../models/index.js";
import type { WorkspaceIndexEmbeddingSchema, WorkspaceIndexStatus, WorkspaceIndexInfo, IndexOptions, IndexResult, SearchPlan, SearchPlanResult } from "../types.js";
export type WorkspaceIndexOptions = {
    mode: "read" | "write";
    embeddingModel?: EmbeddingModel;
};
export declare class WorkspaceIndex {
    readonly info: WorkspaceIndexInfo;
    private readonly storage;
    private readonly embedding;
    private readonly embeddingModel?;
    private closed;
    constructor(info: WorkspaceIndexInfo, options: WorkspaceIndexOptions);
    get name(): string;
    index(options?: IndexOptions): Promise<IndexResult>;
    status(): Promise<WorkspaceIndexStatus>;
    searchPlan(plan: SearchPlan): Promise<SearchPlanResult>;
    close(): void;
    private validateIndexVersion;
    private validateEmbeddingSchema;
    private requireEmbeddingModel;
}
export declare function isWorkspaceIndexed(info: WorkspaceIndexInfo | null | undefined): info is WorkspaceIndexInfo & {
    embedding: WorkspaceIndexEmbeddingSchema;
    indexVersion: number;
};
