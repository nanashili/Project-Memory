export type FileLock = {
    readonly path: string;
    readonly info: FileLockInfo;
    release(): void;
};
export type FileLockInfo = {
    token: string;
    pid: number;
    hostname: string;
    startedAt: number;
    operation: string;
};
type FileLockOptions = {
    operation: string;
    staleMs?: number;
};
export declare function acquireReadWriteLock(lockPath: string, mode: "read" | "write", options: FileLockOptions): FileLock;
export declare function assertNoWriteLock(lockPath: string, operation: string): void;
export {};
