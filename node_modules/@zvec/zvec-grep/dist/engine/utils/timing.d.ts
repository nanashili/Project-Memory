import type { TimingEntry } from "../types.js";
export declare class TimingCollector {
    private readonly entriesByName;
    time<T>(name: string, task: () => Promise<T>): Promise<T>;
    timeSync<T>(name: string, task: () => T): T;
    add(name: string, durationMs: number, count?: number): void;
    addEntries(entries: readonly TimingEntry[] | undefined, prefix?: string): void;
    entries(): TimingEntry[];
}
export declare class ConcurrentTiming {
    private readonly collector;
    private readonly name;
    private active;
    private startedAt;
    constructor(collector: TimingCollector, name: string);
    time<T>(task: () => Promise<T>): Promise<T>;
    private start;
    private stop;
}
