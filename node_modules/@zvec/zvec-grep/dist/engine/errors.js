export const ENGINE_ERROR_CODE_PREFIX = "ZVEC_GREP.ENGINE";
export class EngineError extends Error {
    code;
    context;
    constructor(message, options) {
        super(message, { cause: options.cause });
        this.name = "EngineError";
        this.code = options.code;
        this.context = options.context;
    }
}
export function isEngineError(error) {
    return error instanceof EngineError;
}
export function errorDetails(entries) {
    const lines = entries
        .map(formatDetailEntry)
        .filter((line) => line !== null && line.length > 0);
    return lines.length > 0 ? lines.join("\n") : undefined;
}
export function detail(key, value) {
    return value === null || value === undefined ? null : [key, value];
}
export function workspaceIndexDetail(name) {
    return detail("workspaceIndex", name);
}
function formatDetailEntry(entry) {
    if (entry === null || entry === undefined) {
        return null;
    }
    if (typeof entry === "string") {
        return entry.trim().length > 0 ? entry.trim() : null;
    }
    const [key, value] = entry;
    if (value === null || value === undefined) {
        return null;
    }
    return `${key}=${String(value)}`;
}
//# sourceMappingURL=errors.js.map