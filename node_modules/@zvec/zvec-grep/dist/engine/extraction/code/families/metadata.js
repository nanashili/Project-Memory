const COMMENT_TYPES = new Set([
    "comment",
    "line_comment",
    "block_comment",
    "documentation_comment",
]);
export function extractGenericSignature(node) {
    const body = node.childForFieldName("body") ??
        node.namedChildren.find((child) => child.type === "statement_block" ||
            child.type === "compound_statement" ||
            child.type === "block" ||
            child.type === "class_body" ||
            child.type === "declaration_list" ||
            child.type === "field_declaration_list");
    const text = body && body.startIndex > node.startIndex
        ? node.text.slice(0, body.startIndex - node.startIndex).trimEnd()
        : firstNonEmptyLine(node.text);
    const normalized = text
        .replace(/\s+/g, " ")
        .replace(/\s*[{;]\s*$/, "")
        .trim();
    return normalized.length > 0 ? normalized : undefined;
}
export function extractPrecedingDoc(node) {
    const comments = [];
    let sibling = node.previousNamedSibling;
    while (sibling && COMMENT_TYPES.has(sibling.type)) {
        comments.unshift(cleanCommentText(sibling.text));
        sibling = sibling.previousNamedSibling;
    }
    const doc = comments.join("\n").trim();
    return doc.length > 0 ? doc : undefined;
}
export function extractCommonModifiers(node) {
    const modifiers = new Set();
    const signature = extractGenericSignature(node) ?? firstNonEmptyLine(node.text);
    if (hasAncestor(node, "export_statement")) {
        modifiers.add("exported");
    }
    for (const token of signature.matchAll(/\b(public|private|protected|internal|static|async|pub)\b/g)) {
        modifiers.add(normalizeModifier(token[1]));
    }
    return [...modifiers];
}
export function isInsideNodeType(node, type) {
    let parent = node.parent;
    while (parent) {
        if (parent.type === type) {
            return true;
        }
        parent = parent.parent;
    }
    return false;
}
export function closestAncestor(node, type) {
    let parent = node.parent;
    while (parent) {
        if (parent.type === type) {
            return parent;
        }
        parent = parent.parent;
    }
    return undefined;
}
export function firstNonEmptyLine(text) {
    return (text
        .split(/\r?\n/)
        .find((line) => line.trim().length > 0)
        ?.trim() ?? "");
}
function hasAncestor(node, type) {
    return closestAncestor(node, type) !== undefined;
}
function normalizeModifier(value) {
    return value === "pub" ? "public" : value;
}
function cleanCommentText(text) {
    return text
        .replace(/^\/\*\*?/, "")
        .replace(/\*\/$/, "")
        .replace(/^\/\/\/?\s?/gm, "")
        .replace(/^#\s?/gm, "")
        .replace(/^\s*\*\s?/gm, "")
        .trim();
}
//# sourceMappingURL=metadata.js.map