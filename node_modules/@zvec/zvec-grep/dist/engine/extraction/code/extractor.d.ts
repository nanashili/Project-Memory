import type { EntityFragment } from "../../types.js";
import { type Source } from "../source.js";
import type { ChunkOptions } from "../types.js";
export declare class CodeExtractor {
    extract(source: Source, options?: ChunkOptions): Promise<EntityFragment[]>;
    extractForIndexing(source: Source, options?: ChunkOptions): Promise<PreparedCodeFragment[]>;
    private fallback;
    private extractScriptBlocks;
}
export type PreparedCodeFragment = {
    fragment: EntityFragment;
    embeddingText?: string;
};
