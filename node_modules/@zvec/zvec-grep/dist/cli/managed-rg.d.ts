import type { CliOptions, ParsedArgs } from "./types.js";
export declare function tokenizeManagedRgCommand(command: string): string[];
export declare function parseManagedRgCommand(root: string, command: string): {
    queries: string[];
    options: CliOptions;
};
export declare function normalizeManagedRgInput(parsed: ParsedArgs): {
    queries: string[];
    options: CliOptions;
};
