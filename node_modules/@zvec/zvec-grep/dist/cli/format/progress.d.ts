import type { IndexProgress } from "../../index.js";
import type { ColorMode } from "../types.js";
export type IndexProgressReporter = {
    report(progress: IndexProgress): void;
    reportLine(line: string, force?: boolean): void;
    finish(): void;
};
export type IndexProgressReporterOptions = {
    nonTtyIntervalMs?: number;
    color?: ColorMode;
};
export declare function createIndexProgressReporter(options?: IndexProgressReporterOptions): IndexProgressReporter;
export declare function formatGreenProgressBar(completed: number, total: number, width: number, color: boolean): string;
