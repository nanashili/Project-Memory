import type { RemoteEmbeddingTarget } from "./types.js";
export declare function canonicalizeWorkspaceRoots(roots: readonly string[]): Promise<string[]>;
export declare function workspaceFingerprint(roots: readonly string[]): string;
export declare function remoteEmbeddingTargetFingerprint(input: {
    workspaceFingerprint: string;
    provider: string;
    model: string;
    endpoint: string;
}): string;
export declare function createRemoteEmbeddingTarget(input: {
    roots: readonly string[];
    provider: string;
    model: string;
    endpoint: string;
}): Promise<RemoteEmbeddingTarget>;
