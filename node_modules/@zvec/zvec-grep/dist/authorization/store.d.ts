import { type RemoteEmbeddingAuthorizationStatus, type RemoteEmbeddingTarget, type RemoteEmbeddingWorkspaceGrant } from "./types.js";
export type RemoteEmbeddingAuthorizationStoreOptions = {
    signingKeyPath?: string;
};
export declare class RemoteEmbeddingAuthorizationStore {
    private readonly signingKeyPath;
    constructor(options?: RemoteEmbeddingAuthorizationStoreOptions);
    grantPath(target: Pick<RemoteEmbeddingTarget, "workspaceRoots">): string;
    hasGrant(target: RemoteEmbeddingTarget): Promise<boolean>;
    grant(target: RemoteEmbeddingTarget): Promise<RemoteEmbeddingWorkspaceGrant>;
    revoke(target: RemoteEmbeddingTarget): Promise<boolean>;
    revokeAll(root: string): Promise<number>;
    status(root: string): Promise<RemoteEmbeddingAuthorizationStatus>;
    private readDocument;
    private writeDocument;
    private withDocumentWrite;
    private verifyGrant;
    private readSigningKey;
    private getOrCreateSigningKey;
}
