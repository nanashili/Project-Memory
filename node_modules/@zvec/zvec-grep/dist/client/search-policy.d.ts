import type { CliOptions } from "../cli/types.js";
export type ServerSearchPolicy = {
    freshness: "eventual" | "wait_for_fresh";
    autoUpdate: boolean;
};
export type DirectSearchPolicy = {
    freshness: "eventual" | "wait_for_fresh";
    autoUpdate: boolean;
};
export declare function resolveServerSearchPolicy(options: Pick<CliOptions, "refresh">): ServerSearchPolicy;
export declare function resolveDirectSearchPolicy(options: Pick<CliOptions, "refresh">): DirectSearchPolicy;
