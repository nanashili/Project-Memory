import type { Progress } from "@modelcontextprotocol/client";
type DaemonToolCallOptions = {
    onProgress?: (progress: Progress) => void;
    embeddingEnvironment?: string;
    toolContract?: {
        inputProperties?: readonly string[];
        outputProperties?: readonly string[];
        errorMessage: string;
    };
};
export declare class DaemonClient {
    private readonly options;
    constructor(options: {
        serverUrl: string;
        home?: string;
        tokenFile?: string;
        allowRemote?: boolean;
    });
    callTool(name: string, args: Record<string, unknown>, callOptions?: DaemonToolCallOptions): Promise<Record<string, unknown>>;
    callTextTool(name: string, args: Record<string, unknown>, callOptions?: DaemonToolCallOptions): Promise<string>;
    private invokeTool;
}
export declare function toolSatisfiesContract(tool: {
    inputSchema?: unknown;
    outputSchema?: unknown;
} | null | undefined, contract: {
    inputProperties?: readonly string[];
    outputProperties?: readonly string[];
}): boolean;
export declare function daemonAdminServerUrl(serverUrl: string): URL;
export {};
